{% set flex = grav.get('flex_objects') %}
{% set cities = flex.directory('cities') %}  
{% set collection = cities.getCollection() %}

{% render collection layout: 'default' with {country_code: country_code } %}